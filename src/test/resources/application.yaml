spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fight?useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  ldap:
    urls: ldap://localhost:10389
    base: dc=example,dc=com
    username: zfgican@gmail.com
    password: secret
    embedded:
      ldif: ldap-server.ldif
      base-dn: dc=example,dc=com